<%= link_to 'New Article', new_article_path, class: "btn btn-default btn-lg", id: "new-article-id" %>

<% if @articles.empty? %>
  <h1 id="no-articles">No Articles Created</h1>
<% else %>
  <%= render @articles %>
<% end %>